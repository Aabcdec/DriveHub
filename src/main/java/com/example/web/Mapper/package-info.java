/**
 * MyBatis 数据访问层 — 定义与数据库表对应的 CRUD 及复杂查询方法。
 *
 * <p>每个接口的 SQL 映射文件位于 {@code src/main/resources/Mapper/} 目录下，
 * 文件名与接口名一致（如 {@code TClueDao.java} ↔ {@code TClueDao.xml}）。
 *
 * <p>命名约定：统一以 {@code Dao} 结尾（含 {@code TUserDao}、{@code WxUserDao}）。
 */
package com.example.web.Mapper;
