/**
 * 业务服务接口层（原 Servlet 包，已重命名）。
 *
 * <p>约定：接口位于本包，实现位于 {@link com.example.web.service.impl}，
 * 使用 Spring {@code @Service} 注解。对外 HTTP 路径与 JSON 字段名保持兼容，不因本层重命名而变更。
 */
package com.example.web.service;
